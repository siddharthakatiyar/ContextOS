# Canonical Queries

Run these queries using the ContextOS CLI to evaluate retrieval quality.

```bash
# Target: Interface Implementation Resolution
contextos query "Where is the UserStore interface defined, and how is it implemented in Postgres?"

# Target: Implicit Package Visibility
contextos query "What fields exist on the User struct that the PostgresStore returns?"
```
