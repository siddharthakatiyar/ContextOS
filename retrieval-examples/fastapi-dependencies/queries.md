# Canonical Queries

Run these queries using the ContextOS CLI to evaluate retrieval quality.

```bash
# Target: Decorator/Kwargs Graph Expansion
contextos query "How are users authenticated in the /me route?"

# Target: Pydantic Model Resolution
contextos query "What fields are returned when fetching the current user?"
```
